# 2026-08-20 工作日志

## get_fields 探针结果（reload_diag.log @2026-08-20）：坏窗口期假说**否定**
- 时间线：02:33:29 `get_fields UIVA badState=0`（首次枚举，元数据完好）→ 02:34:22-37 又一次重载（UIVA 同指针复用）→ 02:34:50 `get_fields EntranceWindow badState=0`（首次枚举，完好）→ 02:34:51 `s_currVariable=NULL` NRE。
- **Unity 构建序列化缓存时看到的元数据完全正常，但反序列化仍不写值**。已否定：字段布局错/字段类型陈旧/坏窗口期建空缓存。根因只剩 Unity 原生侧：要么反序列化被**整体跳过**（MonoScript 解析/组件跳过），要么写值路径绕过了我们能看到的地方。
- 注意：get_fields hook 未检查 ReloadDiagEnabled()，但 ReloadDiagLog 内部有门控——日志出现即说明 diag 已启用（此前已有过重载）。

## AllocProbe 分配探针（验证反序列化是否发生）
- 思路：若 Unity 反序列化 `s_currVariable`（VariableArray 对象），必然 `object_new` 创建 VariableArray + List + Variable 对象。**在窗口创建时刻若看不到这些分配 → 反序列化被整体跳过**；若看到 → 对象建了但字段没链接（另一种故事）。
- 实现：`vm/Object.cpp` `Object::New` 挂钩（NewAllocSpecific 前的探针匹配，≤16 指针扫描，开销极小）；`Object::ReloadDiagProbeClass(klass)` 按指针注册（复用类指针跨重载稳定）；`InterpreterImage.cpp` Pass 3 按全名注册 4 个类：EntranceWindow(全局ns)、UIVariableArray(Loxodon.Framework.Views)、VariableArray+Variable(Loxodon.Framework.Views.Variables)。
- 日志：`[ReloadDiag] AllocProbe: new <名> obj= klass= image=`。
- 等用户重跑（不带 JsonUtility 预热，场景 B）：看 02:34:50-51 窗口期有没有 VariableArray/Variable 的 AllocProbe 行。
