# 2026-08-28 工作日志

## Reload 阶段开工：程序集重载 + Adapter 重绑定（第一批，lint 全净，待构建验证）
- **方案（用户确认）**：eager 重绑定（载入完毕后立即做，不用 lazy）+ old 标志做安全网校验；类配对按全名字符串（不依赖指针 key）。
- **改动 1：PlaceHolder 重载路径**（hybridclr/metadata/Assembly.cpp Create 的 token!=0 分支）：原"reloading not supported"异常改为——fresh 创建 newAss/newImage（走 else 分支同款 new）→ 正常 Init/Build/InitRuntimeMetadatas → `MetadataCache::ReplaceInterpreterAssembly(old,new)` 原地替换 → s_placeHolderAssembies 条目更新指向新壳 → `RemapAssemblyReal`+`RemapImageReal` → `OnInterpreterAssemblyReloaded(old,new)` 协调器。补 include il2cpp-api-adapters.h。
- **改动 2：原地替换基础设施**：vm/MetadataCache.{h,cpp} 加 `ReplaceInterpreterAssembly`（s_cliAssemblies 原地换条目，找不到退化注册）；vm/Assembly.{h,cpp} 加 `ReplaceAssembly`（s_Assemblies 原地换 + ++s_assemblyVersion 触发快照重建）。**GetAssemblies 链天然正确**：WrapAssemblyArray 每次重建 adapter 数组；CopyValidAssemblies 按 token!=0 过滤旧壳（旧 ass 已被替换不在列表）。
- **改动 3：AdapterMap 批量 API**（il2cpp-api-adapters.h 模板）：`RemapRealsWhere(filter,pairFn,ud)` 单次持锁批量重绑（pending 收集防迭代器失效）；`MarkRealsWhere`/`ClearStaleMark`/`GetStaleReals` old 标志安全网；原 RemapReal 锁内逻辑抽 `RemapRealLocked`（重绑成功自动清 stale）。**全部 adapter 结构体加 `bool stale` 字段**（Image/Assembly/Field/Method/Property/Event/Domain/Class）。
- **改动 4：类按全名配对**（adapters.cpp 协调器 OnInterpreterAssemblyReloaded）：`BuildClassFullName` 读 typeDef 元数据（typeMetadataHandle→Il2CppTypeDefinition）沿 declaringTypeIndex 嵌套链构造 "Ns.Outer/Inner"（**不触发类加载**；image 反查经 MetadataModule::GetImage(klass->image)）；`GetNewClassNameIndex` 惰性建新 image 已实例化类的全名索引（未实例化无 adapter 无需配对）；流程=MarkRealsWhere 打标 → RemapRealsWhere 按名配对 → GetStaleReals 输出未配对清单（TODO 接日志）。InterpreterImage.h 加 `GetLoadedClassList()`/`GetTypeDefines()` 只读访问器。
- **待办（下迭代）**：①RemapMembersAndTypesForReload——成员按 (parent 配对结果, 名字/签名) 配对（Field/Method/Property/Event 的 map 各自 RemapRealsWhere，filter=real->parent 属旧 image）+ Type 重绑（配对类 byval_arg 的 RemapTypeReal）；②泛型实例 pass（s_GenericClassSet 参数引用旧 image 的重建重绑）；③stale 清单接 ReloadDiagLog；④旧对象保活策略（第一版泄漏换安全）。
- **关键设计点**：类配对只处理"已实例化"的旧类（_classList 非 NULL 项）——adapter 只在 Wrap 时创建，从未跨界的类不存在 adapter，无需配对；新侧同理（未实例化的新类等首次 Wrap 时自然创建 adapter）。

## 热重载崩溃修复（BuildClassFullName，同日）
- **崩溃栈**：PairClassByName→BuildClassFullName :385（GetStringFromRawIndex 处）。
- **根因两个**：①`_typesDefines` 里的 nameIndex/namespaceIndex 是**编码索引**（含 imageIndex 位），直接传给 GetStringFromRawIndex（内部断言 DecodeImageIndex==0 且按裸偏移取字符串）→ 越界读崩溃；②`typeDef.declaringTypeIndex` 实际存的是 **byvalTypeIndex**（InterpreterImage.cpp:1753 `nestedType.declaringTypeIndex = enclosingType.byvalTypeIndex`），不是 typeDef 表索引——沿它走 `_typesDefines[rawIdx]` 会取错行。
- **修复（重写 BuildClassFullName）**：嵌套链改走 **klass->declaringType**（Class::FromName 构建时已解析好的 klass 指针链，无索引歧义）；名字直接用 klass->name（已解析）；仅最外层命名空间需读 typeDef 的 namespaceIndex（先 DecodeMetadataIndex 再 GetStringFromRawIndex）。**教训：读 InterpreterImage 的 _typesDefines 字段时必须区分"编码索引 vs 裸索引"（nameIndex/namespaceIndex 是编码的，declaringTypeIndex 是 byvalTypeIndex 语义）；klass 结构里已解析好的字段（declaringType/name）优先于原始元数据。**

## 成员/Type Adapter 重绑定实施完成（同日，lint 全净，待构建验证）
- **AdapterMap 补 ResolveCurrentReal(real)**：查某 real 当前映射（含重绑后），成员配对用它查"旧 parent 类→新类"。
- **成员配对四组**（adapters.cpp 匿名 ns）：filter=parent/klass 类属旧 image；pair=ResolvePairedClass 得新类 → 惰性 Setup（SetupFields/SetupMethods/SetupProperties/SetupEvents，vm::Class.h:177-181）→ 按名找。**方法配对特殊**：名字+参数个数粗筛（candidates[16]）→ 唯一即中 → 多候选时按参数类型内容比对（type 枚举+byref；CLASS/VALUETYPE/GENERICINST 的 data 指针跨新旧不同，经 Class::FromIl2CppType 解析后按 BuildClassFullName 全名比对——复用类配对的名字构造）。字段/属性/事件按名（同名不可能重载）。
- **RemapMembersForReloadedClasses**：四个成员 map 各自 RemapRealsWhere（Field/Method/Property/Event 的 map 暴露器 GetXxxAdapterMapForReload）。
- **RemapTypesForReloadedClasses**：Type map 是内容键不能用 filter/pairFn 模式——遍历旧 image _classList，ResolvePairedClass 配对成功即 RemapTypeReal(&oldK->byval_arg, &newK->byval_arg)。
- **协调器最终流程**：MarkRealsWhere 打标 → 类按全名重绑 → 成员重绑 → Type 重绑 → GetStaleReals 清单（TODO 日志）。
- **待办**：泛型实例 pass（s_GenericClassSet 参数引用旧 image 的重建重绑）；stale 清单接日志；旧对象保活策略。

## 二次载入 NRE 修复：静态字段迁移（同日，lint 全净，待构建验证）
- **症状**：第二次载入完毕后 `FcWinContainer.Create` NRE at [0x00000]（首参 windowManager=null，来源链是 Loxodon Context/DI 容器=静态字段）。
- **根因**：fresh 新类 static_fields 全零，跨重载状态（单例/容器/Context）全在旧类静态字段里（旧类保活但新代码读新类静态字段=空）。**fresh 类方案的固有缺口，原复用方案靠"复用类"天然规避**。与 08-10 回滚的"静态字段保留"是镜像问题（复用方案下保留出 NRE 被回滚；fresh 方案下不保留出 NRE 需迁移）。
- **修复（MigrateStaticFieldsForReloadedClasses，协调器第 5 步）**：遍历旧 image _classList 配对成功的类——旧类防御性 Init → 新类 Class::Init（跑 cctor 分配 static_fields，单例式 cctor 重建会被旧值覆盖，幂等无害）→ **按字段名+offset 一致**配对静态字段 → 只迁引用类型（CLASS/STRING/SZARRAY/ARRAY/OBJECT/GENERICINST；VALUETYPE 内联存储布局风险跳过；基元留给 cctor；thread static offset<0 跳过）→ **WriteBarrier::GenericStore 写入**（增量 GC 裸写漏脏页标记）。
- **修复中的自查 bug**：初版 switch 把 IL2CPP_TYPE_CLASS 误 continue（单例恰恰是 class 类型静态字段，最需要迁）——已修正 CLASS 迁/VALUETYPE 跳。
- **已知语义**：迁移的引用指向旧对象（旧类保活），新代码经旧对象调实例方法走**旧实现**——可接受的第一步；"旧对象升级到新类"（obj->klass 重写）是后续迭代。布局变化场景（字段 offset 不一致）宁丢勿错。

## 二次载入 NRE 真根因修复 + 静态迁移还原（同日，用户诊断正确）
- **用户反馈**：Create 3 打印了（container=null），NRE 在 container.WindowManager 赋值——**AddComponent 返回 null**，非静态字段问题。要求还原静态迁移。
- **真根因**：GetNewClassNameIndex 只收**已实例化**的新类，但新 image 刚载入类全是惰性的 → 配对失败（FcWinContainer 未实例化）→ 旧 adapter 保持 stale、新代码 typeof(T) 解析新类后 WrapClass 建了**新 adapter**（地址≠引擎 MonoScript 缓存的旧 adapter）→ 引擎从 class 查 MonoScript 失败 → AddComponent 返回 null。
- **修复**：GetNewClassNameIndex 改为遍历新 image **全部类型定义**（GetTypeDefines）逐个 **GetTypeInfoFromTypeDefinitionRawIndex 主动实例化**（幂等）再建索引——全部新类实例化后配对必然成功，WrapClass(新类) 命中重绑后的旧 adapter（地址不变），MonoScript 查找恢复。
- **静态迁移已还原**（MigrateStaticFieldsForReloadedClasses 整体删除，含 WriteBarrier 版本）——按用户要求先验证主线。**注意：fresh 类静态字段全零仍是真实缺口**（单例/容器跨重载状态丢失），主线验证通过后需以独立补丁形式加回（代码在 git 历史/记忆中有完整设计：按名+offset 配对、只迁引用类型、WriteBarrier::GenericStore 写入、CLASS 迁 VALUETYPE 跳）。
- **教训：设计笔记写了"触发新类惰性构建"但实现时退化成"只收已实例化"——实现与设计意图的偏差要在 code review 时对照检查。**
