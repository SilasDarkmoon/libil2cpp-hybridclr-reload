# 2026-08-24 工作日志

## ★★★ image 复用修复成功：不崩溃 + NRE 消失（用户确认）★★★
- 用户确认：本次"复用 image 指针 + token 延迟翻转"修改**成功**——不再崩溃，NRE 也消失。
- **最终方案**（详见 MEMORY.md）：
  1. Assembly.cpp 重载路径 `ass=oldAss; image2=oldAss->image`（复用旧程序集/旧 image 指针）→ `klass->image` = MonoManager 启动时注册的旧 image 指针 → `GetAssemblyIndexFromImage` 命中 → `CanTransferTypeAsNestedObject` 通过 → `s_currVariable` 正常进序列化指令。只 free `image2->name`，不 free `image2->nameNoExt`（raw image 字符串堆内指针）。
  2. **token 延迟翻转**（关键配套）：`BuildIl2CppImage` 不设 `image2->token`（保留旧索引）；`Assembly::Create` 在收集块之后、`InitRuntimeMetadatas` 之前设 `image2->token = image->EncodeWithIndex(0)`（新索引）。解决"太早翻转账致收集阶段 GetImage 路由到错误 image"的崩溃。
- 至此，热重载 MonoBehaviour 序列化字段（嵌套 managed 对象）丢失导致 NRE 的问题**根因已实锤并修复**。

## 日志清理（用户要求，2026-08-24）
- 删除 `MetadataCache.cpp` 写入 `log/assembly_reload_reuse.log` 的遗留调试块。
- 删除 `RuntimeTypeHandle.cpp` 的 `type_is_assignable_from`/`IsInstanceOfType` 失败噪音诊断及 include。
- 删除 `il2cpp-api.cpp` 的 `ImageRegCheck` 及 include。
- 删除 `InterpreterImage.cpp` 的 `VTableSlot-STALE` 返回 null 诊断、InterpClassReused/ReuseRejected、Prewarm 日志（保留 Prewarm 功能本身）。
- 删除 `Reflection.cpp` 的 TypeObjectCreated/GetParamObjects 调查日志、`GenericClass.cpp` 的 RepairStaleGenericInstance/GenericClassCacheMiss 日志、Delegate.cpp 的 CreateDelegate 调查日志；保留功能修复逻辑。
- 当前剩余 ReloadDiagLog 调用仅为 `Interpreter_Execute.cpp` 的 NRE 定位诊断及基础设施；全部改动 lint 通过。
- 跨平台确认：代码改动均为标准 C++，无平台特定分支，应可在 iOS/Android 编译。
