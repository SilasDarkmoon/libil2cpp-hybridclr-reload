# 2026-08-27 工作日志

## PropertyInfo + EventInfo Adapter 实施完成（lint 全净，待构建验证）
- **背景**：MethodInfo（API 层收敛版）用户测试通过后开始。预勘结论：PropertyInfo 7 个 il2cpp-api（2 出 5 入）+ EventInfo 1 个（class_get_events）+ mono-api 5 处 + 1 处直解引用；**icalls 零触点**（RuntimePropertyInfo/EventInfo 全直调 vm::，比 MethodInfo 干净）；debugger 目录无 property 函数。
- **改动（6 文件，标准范式六步）**：
  1. api-types.h：PropertyInfoAdapter/EventInfoAdapter opaque typedef。
  2. adapters.h/.cpp：`PropertyInfoAdapter{const PropertyInfo* real}`、`EventInfoAdapter{const EventInfo* real}` + WrapProperty/UnwrapProperty/RemapPropertyReal、WrapEvent/UnwrapEvent/RemapEventReal（Unwrap 出口 const_cast 交可变指针，同 FieldInfo 范式）。
  3. api-functions.h：8 签名（class_get_events/class_get_properties/class_get_property_from_name 返回值 + 5 个 property_get_* 入参）。
  4. api.cpp：8 处实现；property_get_get/set_method 是"unwrap property → vm 取 MethodInfo → wrap method"的链式转换（跨 adapter 类型协作的首例）。
  5. mono-api.cpp：6 处——mono_class_get_properties(wrap)、mono_property_get_name/parent(unwrap)、mono_property_get_get/set_method(unwrap+wrap method)、mono_custom_attrs_from_property_checked(:1837 直解引用 ->token 修复)。
- **验证**：lint 0 诊断；全库调用方排查=仅 api.cpp 定义本身，无内部调用者（icalls 直调 vm:: 天然无触点）。
- **编译错误修复（用户构建报 C2664）**：vm::Class::GetEvents/GetProperties 返回 const 指针，WrapProperty/WrapEvent 入参原为非 const → 4 处调用编译失败。修复=Wrap 入参统一 const（模板 Wrap 本就收 const RealT*）。**教训：Wrap 入参一律 const RealT*（vm getter 多返回 const）；可变需求只在 Unwrap 出口 const_cast**——此规则并入六步范式。
- **语义注记**：Property/Event 与 FieldInfo 同构——嵌入 klass->properties/events 数组按值分配，RemapReal 热重载配对须按 (parent 类, 成员名)。
- **路线图状态**：Image✓ Assembly✓ FieldInfo✓ MethodInfo✓(API 层收敛版) Property✓ Event✓ → 剩 Il2CppClass + Il2CppType 联动（内容键机制，触点 100+，需先出方案讨论再动手——用户约束）。

## Il2CppDomain + Il2CppException Adapter 实施完成（同日，lint 全净，待构建验证）
- **背景**：Class/Type 攻坚前清小尾巴。预勘发现**异常往返链**（关键约束）：Unity Scripting_Il2Cpp.cpp 存在 exception_from_name_msg 创建→持有→raise_exception 抛出的链路（:136/:160/:175/:197/:239 五处），adapter 必须保证往返一致（find-or-create 天然满足）。
- **改动（8 文件）**：
  1. api-types.h：Il2CppDomainAdapter/Il2CppExceptionAdapter typedef。
  2. adapters.h/.cpp：两结构体 + Wrap/Unwrap/Remap（Exception 的 Unwrap 出口 const_cast 可变）。
  3. api-functions.h：domain 3 签名 + exception 7 签名 + thread_attach 入参 + **3 个异常出参**（runtime_invoke×2/runtime_object_init_exception 的 Il2CppException**→Il2CppExceptionAdapter**）。
  4. api.cpp：实现同步；native_stack_trace 直解引用 ex->native_trace_ips 修复（unwrap 后访问）；**异常出参桥接模式**：局部 Il2CppException* realExc=NULL → vm 调用 → *exc=WrapException(realExc)。
  5. 内部调用者 4 处改 vm 直调：vm/ThreadPoolMs.cpp:47→vm::Exception::Raise；icalls/RuntimeType.cpp:839/:846/:855→vm::Exception::Raise（replace_all 一次完成）。
  6. mono-api：mono_get_root_domain(wrap 出)、mono_thread_attach(unwrap 入)、mono_runtime_try_invoke(method unwrap+exc 出参桥接 wrap)。mono_domain_set_fast/foreach 经 mono_get_root_domain 间接比较自动一致；lock/unlock 空操作无需改。
- **新范式沉淀——出参桥接**：API 的指针出参（如 Il2CppException** exc）不能透传给 vm（会写真实指针），须局部变量中转+出口 wrap。这是"往返链"在出参上的变体，后续 Class/Type 阶段的出参（如 Il2CppType**）同理。
- **欠账新增**：debugger/il2cpp-api-debugger.cpp 的 domain 函数直解引用 ((Il2CppDomain*)domain)->agent_info/friendly_name（MONO_DEBUGGER 构建，默认关）。
- **教训**：中途一条回复出现乱码（模型输出异常），用户要求重新检查——恢复后先核实文件真实状态再继续，未造成重复编辑。
- **编译错误修复（C2440）**：il2cpp_domain_get 签名返回 Il2CppDomainAdapter*（非 const，原 API 即如此），WrapDomain 原返回 const → 丢失限定符。修复=WrapDomain 返回非 const（模板 Wrap 返回 AdapterT* 本就非 const）。**const 规则完整版：Wrap 入参 const RealT*、返回 AdapterT*（const 性跟随 API 签名）；Unwrap 入参 const AdapterT*、返回按 API 需求（可变则 const_cast）**。

## Il2CppClass Adapter 阶段 A 实施完成（同日，lint 全净，待构建验证——最大的一块）
- **预勘查证（userdata 偏移）**：Unity 侧链路=ScriptingManager.cpp:213 经 API 写入 → MonoManager_Il2Cpp.cpp:276 启动时经 API 取偏移 → GetComponent.cpp:219 按偏移直读（唯一绕 API 直访点）。libil2cpp 内部对 unity_user_data 引用仅 API 两处，真实类体字段可闲置。修法=Il2CppClassAdapter 加 userdata 槽，offset API 返回 offsetof(adapter, userdata)。
- **改动（~17 文件）**：
  1. api-types.h：Il2CppClassAdapter typedef + Il2CppProfileAllocFuncAdapter 回调（旧 typedef 不动供 vm）。
  2. adapters.h/.cpp：`Il2CppClassAdapter{const Il2CppClass* real; void* userdata;}` + WrapClass/UnwrapClass/RemapClassReal + Get/SetClassUserdata。**RemapReal 换 real 时 userdata 槽不动（Unity::Type* 跨重载保留）**。
  3. api-functions.h：~60 签名（class 家族 45+、array 6、跨家族携带 15：field/method/property/object/custom_attrs/liveness/type/image、userdata、for_each 回调参数、profiler_install_allocation）。
  4. api.cpp：实现同步；**userdata offset 改返回 offsetof(Il2CppClassAdapter, userdata)**（offsetof 限定名不能带 struct 关键字）；**class_for_each 回调 bridge**（ClassForEachBridgeContext+桥函数，vm 遍历真实类→wrap→用户回调）；**profiler alloc bridge**（单槽 s_ProfilerAllocCallback+ProfilerAllocBridge 注册给 vm，与 enter/leave 同模式）；class_get_type 保持返回真实 &real->byval_arg（阶段 B 欠账，注释标明）；直解引用修复：is_inited->initialized、get_type_token->token、get_rank->rank、get_data_size->static_fields_size、get_static_field_data->static_fields、custom_attrs_from_class->image/token。
  5. mono-api.cpp ~40 处：三类模式——转发型（cast 改 (Il2CppClassAdapter*)，18 处）、直调型（UnwrapClass 后传 vm，15 处）、直解引用型（vtable/generic_class/element_size/parent 遍历/custom_attrs token，7 处）；**methods_by_name 迭代重构**：klass 变量改 adapter 域（迭代/比较用 adapter 唯一性，父类遍历经 il2cpp_class_get_parent 保持 adapter 域，goto handle_parent 兼容）；返回真实 klass 的 getter 补 WrapClass（type_get_class/method_get_class/field_get_parent/property_get_parent/get_string_class/get_byte_class/from_mono_type_internal/from_generic_parameter/nested_types/create_array）。
  6. 内部调用者 13 处改 vm 直调：vm/ThreadPoolMs.cpp:104/:63、icalls/MonoMethodMessage×3、CultureInfo/CultureData×2/CalendarData（array_class_get+array_new_specific 组合）、RuntimeMethodInfo×2、EventInfo×1、RuntimeType×3（from_il2cpp_type/array_class_get）。RuntimeMethodInfo.cpp:35 的 il2cpp_class_get_generic_type_definition 是文件内同名 static 函数（收真实 klass）不用改。4 个 icall 文件补 include vm/Class.h（CultureData 补 vm/Array.h）。
- **欠账**：①class_get_type/method_get_return_type/field_get_type 等仍返回真实 Il2CppType*（阶段 B 处理）；②gc/WriteBarrierValidation.cpp（默认关宏）；③debugger/ 目录（MONO_DEBUGGER 构建，含 VM_CLASS_IS_INTERFACE 宏）。
- **热重载接入语义**：重载时 RemapClassReal(oldKlass,newKlass)——userdata 槽保留；klass->image/parent 等内部指针不变（内部继续真实指针）。

## 阶段 A 编译错误修复（C2733/C2039，同日）
- **错误 1（C2733 array_element_size）**：该符号原有**两个定义**长期共存——il2cpp-api.cpp（const Il2CppClass*）与 vm/Array.cpp:292（LIBIL2CPP_CODEGEN_API 版）。改 api.cpp 签名后与 vm/Array.h:41 的 extern "C" 声明（旧签名）冲突。**查证 Unity 消费链**：scripting_array_element_size（ScriptingApi:734）→ ManagedObject.h:130 传 scripting_object_get_class 结果（=il2cpp_object_get_class=已 adapter 化）→ **Unity 传的是 adapter，该 API 必须收 adapter**。**修法**：符号唯一定义收敛到 vm/Array.cpp:292 改收 adapter（UnwrapClass 转发 GetElementSize）+ Array.h:41 声明同步 + api.cpp 删重复定义 + 内部调用者 13 处改 vm::Array::GetElementSize 直调（vm/Array.cpp×2、Marshal.cpp×5、icalls/Array.cpp×5）。**教训：改 API 签名前先查该符号是否在 vm 头文件有 extern "C" 重复声明/定义（LIBIL2CPP_CODEGEN_API 标记的符号要特别排查）——这类"符号共享"点必须整体迁移，不能只改 api.cpp。**
- **错误 2（C2039 offsetof）**：Il2CppClassAdapter 定义在全局作用域（adapters.h 的 struct 在 namespace 外），offsetof(il2cpp::api::Il2CppClassAdapter,...) 限定错误 → 去掉命名空间限定。
- **用户指示修正（array_element_size 归位 api.cpp）**：用户要求 API 边界函数实现应在 api.cpp、删除 Array.cpp 的 extern 与实现。已执行：api.cpp 恢复唯一定义（收 adapter，注释记录历史双定义缘由）；vm/Array.cpp 删 LIBIL2CPP_CODEGEN_API 定义；vm/Array.h 删 extern "C" 声明块。全库验证：仅剩 api.cpp 定义+api-functions.h 声明，无调用者依赖被删声明（内部 13 处已改 vm::Array::GetElementSize 直调）。**原则：API 边界函数住 API 层；vm 头文件的 extern "C" 声明是历史错位，迁移时整体清除。**
- **漏网修复（用户构建报 C2664，array_new/object_new 传真实 klass）**：CultureInfo.cpp:206/:222、ActivationServices.cpp:38、RuntimeType.cpp:740/:1083/:1084 共 6 处改 vm 直调（vm::Array::New / vm::Object::New），3 文件补 include（CultureInfo+ActivationServices 补 vm/Object.h，RuntimeType 补 vm/Object.h）。**教训：icalls 里传 il2cpp_defaults.xxx_class 或本地 Il2CppClass* 变量给导出 API 的点，预勘正则要覆盖 array_new/object_new/value_box/runtime_class_init 等构造类 API，不能只搜 getter 家族。**
- **漏网修复二（array_new_specific 家族）**：MonoMethodMessage.cpp:60/:64/:76、vm/ThreadPoolMs.cpp:111 共 4 处改 vm::Array::NewSpecific 直调。至此 array 构造家族（new/new_specific/new_full/element_size）内部调用者全部清零。
- **漏网修复三（bounded_array_class_get）**：RuntimeType.cpp:870（make_array_type 的 rank>0 分支）改 vm::Class::GetBoundedArrayClass 直调。
- **重大事故修复（运行期 8 符号 not found，用户定位"class adapter 之前是好的"）**：class 阶段漏改 api-functions.h 中 8 个 DO_API 声明的入参（class_get_events/fields/properties/property_from_name/field_from_name/methods/method_from_name/image 仍是 Il2CppClass*），api.cpp 定义已是 Il2CppClassAdapter*。**机制**：C++ 参数类型不同=合法重载 → C 链接符号给了无定义的声明、定义获得 C++ 修饰名 → 导出表无裸名 → LookupSymbol 失败。编译链接全通过（静默），运行期才爆。**为何之前阶段没踩坑：改返回值不参与重载决议（不一致会编译报错），改参数会静默变重载**。修复=8 声明同步+全量核验 api-functions.h 裸 Il2CppClass* 零残留。**铁律：改 API 参数类型时，DO_API 声明与 api.cpp 定义必须在同一编辑批次成对修改；改完全量搜 api-functions.h 中该类型残留做核验。排查手段：git diff 的上下文行（空格开头）中出现本应改动的签名=漏改。**

## Il2CppType Adapter（阶段 B，内容键机制）实施完成（同日，lint 全净，待构建验证——最后一块拼图）
- **Class 阶段 A 用户验证通过后开始**。机制与前序不同：Il2CppType 按值嵌入（byval_arg/type_argv/方法签名）无独立身份 → **内容键映射**。
- **设计**：`Il2CppTypeAdapter{const Il2CppType* real;}`；键=(type枚举,byref,attrs,data 裸指针值)（num_mods/pinned 恒 0 忽略，valuetype 位由 type 推导；data 经 union 首字段 dummy 按字节读）；双索引 map（键→adapter 供 find-or-create，adapter→键供 Remap 反查）；同一逻辑类型（多次 inflate 的不同实例）同一 adapter。**RemapTypeReal(old,new)**：重载流程按 klass 配对调（&oldK->byval_arg→&newK->byval_arg），O(n) 遍历重载低频可接受；泛型实例按 genericClass 配对同理。
- **改动（6 文件）**：①api-types.h typedef ②adapters.h/.cpp 专用内容键 map（不复用指针键 AdapterMap 模板；cpp 补 include il2cpp-runtime-metadata.h）③api-functions.h 19 签名（出 5：class_enum_basetype/class_get_type/field_get_type/method_get_return_type/method_get_param；入 14：class_from_il2cpp_type/class_from_type/type_* 12 个）④api.cpp 19 处（同批成对修改+核验零残留——铁律执行）⑤mono-api ~20 处：signature->ret/params、header->locals（边界结构填充 Wrap）、type_get_class/is_struct/is_reference/generic_inst_is_valuetype/full_name/get_name_full/get_object/from_mono_type/generic_parameter/size（Unwrap）、get_type/is_byref/get_attrs 转发 cast、get_byref_type/field_get_type/object_get_type/get_void_type/get_object_type（Wrap）。⑥icalls 零触点（RuntimeTypeHandle.cpp:258 是 vm::il2cpp_type_from_type_info 非 API）；debugger 无 type 函数无新欠账。
- **const 修正 2 处**：FromIl2CppType/GetTypeSizeAndAlignment 均收 const，去掉多余 const_cast。
- **欠账（Type 特有，重要）**：①Il2CppReflectionType.type（RuntimeType.value）托管堆存真实指针——icall Reflection::GetTypeObject(Unwrap 后的 real) 写入，不经 API 边界，adapter 够不着（与 Module/Assembly/Method 的 mhandle 同类）；②PTR/SZARRAY/GENERICINST 的组合类型：内容键含 data 裸指针（元素 type 指针/genericClass 指针），重载后这些子指针也换——Remap 清单需覆盖（第一版接口已备，配对逻辑归重载流程）；③泛型 inflate 每次新分配 Il2CppType——内容键使它们共享 adapter，但 real 只指向首个实例，后续实例地址不同内容相同（可接受：Unwrap 返回任一内容等价实例）。
- **Adapter 实验全图完成**：Image✓ Assembly✓ Field✓ Method✓(含 StackFrameInfo 平行结构) Property✓ Event✓ Domain✓ Exception✓(含出参桥接) Class✓(含 userdata 槽+回调 bridge) Type✓(内容键)。

## 欠账清理（同日，用户要求盘点并修复）
- **已修复（非默认编译路径 2 处，lint 全净）**：①gc/WriteBarrierValidation.cpp GetReadableStackTrace 4 处 API 调用改 vm 直调（Method::GetDeclaringType/Class::GetNamespace/GetName/Method::GetName，补 include vm/Class.h+vm/Method.h）——内部栈帧 method 是真实指针，传 adapter API 会错。②debugger/il2cpp-api-debugger.cpp ~30 处全量修复：domain agent_info/friendly_name×4(UnwrapDomain+const_cast)、method 群×7(UnwrapMethod/WrapMethod)、defaults getter×5(WrapClass/WrapImage)、image_name/get_sequence_point/debug_get_source_file×3(UnwrapImage)、field 群×4(UnwrapField)、iterate_loaded_classes×2 返回点(WrapClass)、interface_method(unwrap 双参+wrap 出)、generic 群×4、assembly×2(UnwrapAssembly)、type 群×4(WrapType/UnwrapType，InflateIfNeeded 收 const 去 const_cast)、class_is_initialized(UnwrapClass)。补 include il2cpp-api-adapters.h（回滚时丢了）。残留扫描零命中。
- **不修（架构边界，实验起点已识别）**：托管堆裸指针 5 处——Il2CppReflectionType.type(RuntimeType.value)/Il2CppReflectionMethod.method(mhandle)/Il2CppReflectionAssembly.assembly/Il2CppReflectionModule.image/RuntimeFieldInfo.fieldHandle。icall 写入不经 API 边界，adapter 够不着；无重载时不陈旧（实验验证不受影响），有重载时属重载流程职责（旧对象复用或接受内容等价）。
- **归热重载接入（接口已备）**：Remap 编排（谁按什么顺序调 10 个 Remap*）、Type 组合类型子指针配对（PTR/SZARRAY/GENERICINST 的 data）、泛型实例配对、Field/Property/Event 按 (parent,名) 配对。
- **欠账清零状态**：代码级欠账全部清零；剩余为架构已知边界（文档化）与重载编排（下一阶段工作）。

## 重大回退：Il2CppException 整体取消 Adapter 化（退出时偶发崩溃根因，同日）
- **症状**：关闭 exe 时偶发崩溃，栈顶 StackTraceUtility_ExtractStringFromExceptionInternal ← Runtime::InvokeWithThrow ← il2cpp_runtime_invoke。
- **根因链（完整查证）**：Unity 侧把 Il2CppException 当**托管对象**用，不是纯句柄——①ScriptingBackendNativeExceptionPtr=ScriptingReferenceWrapper<Il2CppException*>，构造/赋值走 scripting_gc_wbarrier_set_field→il2cpp_gc_wbarrier_set_field→GC_END_STUBBORN_CHANGE→GC_dirty（gc_priv.h:2196：GC_incremental 开启时 GC_dirty_inner 查页表，adapter 是 IL2CPP_CALLOC 的堆外内存→越界崩；关闭时空操作不崩——**"有时崩有时不崩"的机制**）②ExtractStringFromExceptionInternal 经 ScriptingInvocation.AddObject 把异常传给反射调用（adapter 无 Il2CppObject 头→崩）。触发条件=退出路径恰好有异常要提取+增量 GC 状态，故偶发。
- **修复（整体回退）**：exception 全家族（raise/from_name_msg/argument_null/format×2/unhandled/native_stack_trace/runtime_invoke×2/object_init_exception 出参+mono_runtime_try_invoke）签名与实现回退真实指针；adapters.h/.cpp 删除 Il2CppExceptionAdapter/Wrap/Unwrap/Remap；api-types.h typedef 换成"不做 Adapter 化"注释。全库残留零，lint 全净。
- **教训（并入范式）**：**判据=Unity 侧是否把该指针"当对象用"**（写屏障包装/反射传参/ToObject 转换）——是则不能 adapter 化。Il2CppObject 系（Exception/String/Array 等托管对象）天然不可 adapter；Image/Class/Method 等纯元数据句柄可以。**出参桥接模式（局部中转+出口 wrap）只适用于纯句柄出参，不适用于对象出参**。
- **Adapter 实验最终版图**：Image✓ Assembly✓ Field✓ Method✓ Property✓ Event✓ Domain✓ Class✓ Type✓（Exception✗ 回退——对象型指针不可 adapter 化）。
- **漏网修复四（mono/ 目录盲区）**：mono/ThreadPool/threadpool-ms.cpp:120 的 il2cpp_class_from_type 改 vm::Class::FromIl2CppType 直调（vm/Class.h 已 include）。**教训：预勘目录清单要包含 mono/（14 文件，ThreadPool 等 Mono 兼容层），不能只扫 vm/icalls/hybridclr**。mono/ 其余调用均为安全宏（il2cpp_array_setref/IL2CPP_OBJECT_SETREF），已清零。
