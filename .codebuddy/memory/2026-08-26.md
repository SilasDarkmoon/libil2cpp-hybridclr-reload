# 2026-08-26 工作日志

## Adapter 实验进展（当日早些时候，均已被用户验证/处理）
- Il2CppImage adapter 完成并验证通过（强类型版）→ Il2CppAssembly adapter 完成并验证通过 → FieldInfo adapter 完成（含 C2440 const 修复：adapter 结构体 real 成员必须统一 const RealT*，可变需求在 Unwrap 出口 const_cast）。
- MethodInfo 大改（18 文件版：改 Il2CppStackFrameInfo.method 字段类型+vm/Profiler/StackTrace/Exception/hybridclr Engine/codegen/debugger/vm-utils 全动）——**被用户整体回滚**。

## 用户回滚 + 新约束（长期有效）
- **回滚原因**：改动面过大（vm/、hybridclr/、codegen/、debugger/、vm-utils/ 等内部源文件被动）。
- **新约束**：改动限制在 API 接口层（api-types.h / api-functions.h / api.cpp / adapters.h/.cpp / mono-api.cpp + 编译强制的 icalls 小修）；**不动 vm/ 等内部源文件；涉及面过大必须先问再动手**。

## MethodInfo Adapter + Il2CppStackFrameInfoAdapter（API 层收敛版，已实施，lint 全净，待构建验证）
- **方案（用户指定）**：并行结构——il2cpp 内部继续用原 Il2CppStackFrameInfo（method=真实 MethodInfo*），仅 API 出口转换成 Il2CppStackFrameInfoAdapter（method=MethodInfoAdapter*）。
- **改动 7 文件**：
  1. **api-types.h**：MethodInfoAdapter typedef；Il2CppStackFrameInfoAdapter（**布局镜像**内部结构——Unity 预编译代码按旧结构读写偏移 0 的 method 指针兼容，读到的实为 adapter 指针，传回 method API 正确）；Il2CppFrameWalkFuncAdapter / Il2CppProfileMethodFuncAdapter 新回调类型（**旧 typedef 不动**——改共享 typedef 会波及 vm/StackTrace.h/Thread.h/Profiler.h 编译）。
  2. **adapters.h/.cpp**：MethodInfoAdapter{const MethodInfo* real} + WrapMethod/UnwrapMethod/RemapMethodReal + WrapStackFrameInfo 逐字段转换（adapters.h 新增 include il2cpp-api-types.h）。
  3. **api-functions.h**：26 个 method API 签名 + 4 栈帧 API（get_top_frame×2/get_frame_at×2 收 Il2CppStackFrameInfoAdapter*）+ 2 walk API（收 Il2CppFrameWalkFuncAdapter）+ profiler_install_enter_leave（收 Il2CppProfileMethodFuncAdapter）。
  4. **api.cpp**：26 method 实现（wrap/unwrap）；**walk trampoline**（FrameWalkBridge 收 vm 内部结构→WrapStackFrameInfo→调用户 adapter 回调，FrameWalkBridgeContext 存用户回调+userData）；**profiler bridge**（单槽全局 s_ProfilerMethodEnter/LeaveCallback + ProfilerMethodEnter/LeaveBridge 注册给 vm::Profiler::InstallEnterLeave——Unity 实际单 profiler 注册，多注册后覆盖前者=与 vm back() 语义一致）；4 栈帧 API 用局部 Il2CppStackFrameInfo 中转。
  5. **mono-api.cpp** ~20 站点：class_get_methods(wrap)、class_get_methods_by_name(迭代重构：adapter/real 分离、数组存 adapter)、inflate×2(unwrap+wrap)、signature_internal、debug_lookup_locals×3+find_method、get_header_checked、param_names、context、full_name、generic_container、get_name/class/flags/token/is_generic/is_inflated、object_get_virtual_method(unwrap+wrap)、verifier(直解引用修复)、custom_attrs_from_method_checked(直解引用修复)。**无需改**：mono_image_get_entry_point(内部读 token 不出边界)、mono_runtime_invoke_checked(断言桩)、:744 转发 cast(马甲语义正确)。
  6. **icalls 2 文件（编译强制，改 vm 直调）**：RuntimeMethodInfo.cpp:266→vm::Reflection::GetMethodObject；RuntimeType.cpp:1077/1119→vm::Class::GetMethods、:1122/:1144/:1150→vm::Reflection::GetMethodObject。
- **欠账**：①gc/WriteBarrierValidation.cpp:226/:231（IL2CPP_ENABLE_WRITE_BARRIER_VALIDATION 默认关，开启需 2 行 Unwrap）；②debugger/il2cpp-api-debugger.cpp ~10 处 method 函数（MONO_DEBUGGER 构建运行期语义待同步）；③Il2CppReflectionMethod.mhandle 托管裸指针；④method_get_return_type/get_param 仍返回真实 Il2CppType*。
- **架构对比教训**：改结构体字段类型=编译器强制但波及全部解引用点（vm/hybridclr/codegen 全中招）；平行 Adapter 结构+出口转换=零内部波及但依赖出口清单完备（出口=API 函数+回调触发点，可枚举）。用户选后者。
