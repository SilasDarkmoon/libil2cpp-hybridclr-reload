# 2026-08-31 工作日志

## 里程碑：Adapter 热重载初步测试通过（用户确认）
- 2026-08-28 实施的完整链路（PlaceHolder fresh 载入+原地替换 → 协调器类/成员/Type 重绑 → 全类型主动实例化修复 AddComponent null）经用户初步测试可行。
- MEMORY.md 全面更新：Adapter 最终版图（九类完成+Exception 回退）、热重载实现细节、关键修复与教训、待办清单归档为长期记忆。
- 当前状态：主线（类/成员/Type 重绑+MonoScript 缓存兼容）可用。

## 待办大幅简化：用户给出两条豁免前提（同日讨论）
- **用户约定：重载后不再使用旧对象（含反射对象/泛型集合/静态缓存）**——新代码从自身初始化路径重建全部状态（测试已验证框架初始化幂等：FcWinContainer 链路重建成功）。
- **豁免 1：托管堆裸指针 5 处**——旧反射包装对象（Type/MethodBase/Assembly/FieldInfo）不用则内部旧指针永不被解引用；GC 只当指针槽追踪不解引用语义。移出待办。
- **豁免 2：静态字段迁移**——旧类静态字段（单例/容器/Context）随旧世界退役；新类 fresh 静态字段由新 cctor 重建。补丁取消（设计仍存档 2026-08-28 日志，若约定破坏可取回）。
- **泛型实例 pass 简化**：跨重载存活的泛型实例若只被旧世界持有则同样退役；新代码构建新实例天然正确。残余风险=AOT 侧缓存实例（s_GenericClassSet）被新代码命中——降级为**先观测日志**（遍历 set 打印参数引用旧 image 的实例），有命中再做重绑。
- **待办最终收敛**：stale 清单日志（1）→ 泛型实例观测日志（2）→ 保活策略（3，最低优先级）。
- **使用约定（需写进项目文档）**：重载后不得使用重载前创建的托管对象（含反射对象、泛型集合、事件订阅、静态缓存）——新代码从自身初始化路径重建全部状态。

## 泛型实例风险面最终评估（同日续，用户追问"Unity 缓存都是非泛型类型可否无视"）
- **用户判断成立的核心**：MonoBehaviour/ScriptableObject 脚本类不可能声明为泛型（C# 语言限制）→ Unity 引擎长期缓存的类指针（MonoScript 绑定/m_ScriptImages/类型表）全是非泛型，类配对 pass 已覆盖。
- **残余风险三点评估**：①用户旧泛型集合=豁免约定覆盖；②AOT s_GenericClassSet 死条目=新代码内容键查找不命中旧实例（typeHandle 不同→创建新实例），天然正确只费内存；③**唯一真实残余=序列化指令缓存里嵌的泛型元素类型**——指令 key（FieldInfo adapter）重绑但内容不重建，嵌的泛型实例 adapter stale：HotClass 布局不变则行为一致无害；**布局变化+作为 List<T>/数组元素被序列化→新实例按旧布局读写→字段错位**。
- **结论**：可无视，触发条件明确（热更类型布局变化+序列化元素场景），命中症状明确（该 List 字段数据错乱），届时再做泛型 pass。观测日志保留（与 stale 日志可合并一次改动）。
- **待办最终版**：stale 日志+泛型观测日志（合并，1）→ 保活策略（2）。
