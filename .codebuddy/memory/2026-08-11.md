# 2026-08-11 工作日志

## 背景（延续 08-10）
NRE 根因已收敛：`EntranceWindow.OnCreate` 中 `GetAttrib<UIVariableArray>("pveView"/"layoutSwitchers")` 在重载后（场景 B：类型未预热）返回 null → `Bind(null)` → `For(null)` NRE。字段布局/vtable/方法体均已排除。GetAttrib 失败三候选：A) variables 为 null/空（反序列化失败）；B) 名字不匹配；C) `IsAssignableFrom` 类型失配（重复 RuntimeType 同源）。

## 用户要求
- 业务 C# 改动打包测试太慢 → 诊断全部做在原生层（重编 libil2cpp 即可）。
- 批准两项改动：①IsInstanceOfType 失败日志改纯内存读取；②Bind 第一个参数 target 的来源追踪。③UIVariableArray.Get 挂钩暂缓。
- 之前写入 Downloads 的 C# 埋点文件（EntranceWindow.cs/VariableArray.cs/UIVariableArray.cs）作废——注意：曾误删/误并文件，已恢复；write_to_file 对 Downloads 路径可用，replace_in_file 曾出现"假成功"（疑与损坏的 PowerShell profile 环境有关）。

## 本次原生改动（lint 均通过）
1. **RuntimeTypeHandle.cpp**：`ReloadDiagLogClassMismatch` 改为纯内存读取（name/namespaze/image 指针，无 GetTypeDefinition/class_from_type），移除 Backpack/HomeWindow 过滤——**每次失败都打印**，加 64 槽指针去重环防刷屏。覆盖 `IsInstanceOfType` 与 `type_is_assignable_from`（`typeof(T).IsAssignableFrom(v.VariableType)` 走后者）。判读：若 GetAttrib 返 null 时日志出现 UIVariableArray 相关 FALSE → 假设 C（类型失配）；若无 → A/B（variables 空/名字不匹配）。
2. **Interpreter_Execute.cpp**：新增 `ReloadDiagScanSlotWriters(imi, ipBase, scanEnd, slot, tag)` 助手（放在 GET_OBJECT_VIRTUAL_METHOD 后）——回溯扫描槽位写入指令，call 类解析 callee（偏移与故障解码一致），纯内存读取+codeLength 边界。BindVCall 挂钩中 **target 为 null 时**调它扫 `argBase+1` 槽（tag=BindArgSource）。关键点：在 **Bind 调用点**扫描（帧槽位仍存活），而非 catch 时（槽已被复用——之前 vdispatch 失败的教训）。

## 等用户重跑
看 `BindArgSource` 行（producer 应为 GetAttrib 调用，确认链条）+ `type_is_assignable_from FALSE` 行（分辨假设 C vs A/B）。

## UIVariableArray.cs 埋点（用户还原后要求最小改动、单文件、自包含 logger）
- 用户还原了 UIVariableArray.cs，看到真实源码：**`s_currVariable` 是 `[SerializeField]`**（Unity 反序列化填充），`_currVariableMap` 是运行时缓存 Dictionary。
- `GetAttribBase` 三个提前返回点：`IsDestroyed()` / `s_currVariable == null` / name 空；然后 `s_currVariable.Get<T>(name)`（VariableArray.Get：FindVariable 找名字 → `GetValue<T>() as T`）。
- 埋点（自包含 `UIVADiag` 嵌套类，写 reload_diag.log，按 实例+name 去重）：
  1. **early-out 时**打印 `destroyed=` / `s_currVariable=NULL|ok` / name / T / go 名 → 区分"Unity 对象被销毁" vs "序列化字段没填"；
  2. **`Get<T>` 返回 null 时** dump `Variables` 的 count + 每条目 `Name(vt=VariableType)` → 区分"列表空（反序列化失败）" vs "名字不匹配" vs "类型不匹配"。
- 只依赖本文件已证实存在的成员（`Variables` 属性、`Variable.Name`/`VariableType`），不依赖隐式转换。
- 注意：Downloads 下文件修改曾出现 replace_in_file"假成功"问题，写后必须 search_content/read_file 验证。

## 决定性证据补全（reload_diag(1).log）
- **DeserProbe @11:51:32**（重载期）：EntranceWindow 字段全正常（12 字段、size_inited=1、typeHandle 有效、image=RuntimeScripts.dll）。`_resLoadPool/_goalCupRewardItems` 等是运行时字段非 SerializeField。
- **ReusedClassChain @11:51:18**（重载完成时）：`UIVariableArray` 元数据恢复完好——`s_currVariable offset=32 type=18 attrs=0x1`、`_currVariableMap offset=40 type=21`，链到 UIBehaviour/MonoBehaviour 都正常。
- **NRE @11:51:59**：`s_currVariable=NULL`。即**元数据 40 秒前就正常，但运行时反序列化没填值**。
- **缓存窗口假说被时间线否定**（Pass2 fields=null 与反序列化之间隔了 40+ 秒，中间 Pass3 已重建好）。
- 结论收敛：复用类的**类元数据完全正常**，问题是 **Unity 反序列化按"类型关联"路径没把数据写到这个复用类实例**。Unity 原生侧（SerializedFile→native C++）按 GUID 找 C# 类型；对 HybridCLR 解释类型，若其类型注册/MonoScript 关联在重载后失效或指向空，反序列化静默跳过。
- **新增 thisDump 诊断**（Interpreter_Execute.cpp catch 块 NRE 处）：dump `this`（slot 0 对象）的 `klass` 指针+全字段字节（offset16..instance_size，每行 8 字节 hex）。下跑看 `+032`（s_currVariable）处字节：全 0=反序列化没写；非 0 但解释器读到 null=偏移/klass 不一致（读错位置）。`klass` 指针可与 ReusedClassChain 的复用类指针对照。

## 决定性证据（reload_diag.log @ Downloads，11:41:01）+ 假设 C 排除
- **完整链条闭环**：`UIVA.GetAttrib early-out name=pveView/layoutSwitchers/pveGo destroyed=False s_currVariable=NULL go=EntranceWindow` → `BindVCall target=null` → `BindArgSource: slot=21 writer[2] callee=UIVariableArray::GetAttrib`（null 确认来自 GetAttrib）→ NRE。
- **根因实锤：`s_currVariable`（`[SerializeField]`，UIVariableArray 在 UIVariable.cs 的字段）在重载后是 null——Unity 反序列化没填充它**。对象没被销毁（destroyed=False）。
- **假设 C（重复类型）排除**：全日志 `type_is_assignable_from FALSE` 都是正常属性反射失败（JsonIgnoreAttribute vs OptionalAttribute 等），无 UIVariableArray/GameObject/重复类型迹象。
- 正常 Get-null（如 MainNavigation_Landscape 查 tasksBtnCtrl 名字不在列表）是设计性查询，属正常。
- **反序列化失败机制**（待证）：Pass 2 把复用类 `fields=null`，Unity 序列化缓存若在 Pass2/3 之间读取复用类的 fields → 捕获空 → 字段不填。pass 3 重置 init flags（2399-2409）有 Class::Init 每次重建。
- **新增 DeserProbe 诊断**（InterpreterImage.cpp Pass 3 Class::Init 后，仅 EntranceWindow 一个类）：转储可序列化引用字段（CLASS/STRING/SZARRAY/GENERICINST/ARRAY）的 name/offset/type/typeHandle/image。Unity 反序列化解析字段类型走 `Class::FromIl2CppType`（带 assert，**崩溃也是诊断信号**）。下一跑：无 DeserProbe 行=类未 Class::Init（严重）；有行但 typeHandle 异常=陈旧类型；行正常=fields 对，问题在别处（Unity 缓存/`s_currVariable` 父类链的 VariableArray）。
